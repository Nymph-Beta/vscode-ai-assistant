---
name: 前端消息清除同步
overview: 添加从后端到前端的消息清除通知，使命令面板切换模式时能同步清除 UI 聊天历史。
todos:
  - id: add-clear-message
    content: 在 notifyModeChanged() 中添加发送 xiaoke.webview.history.clear 消息
    status: completed
  - id: handle-clear-message
    content: 在 App3.vue 的 vscodeListener 中处理 xiaoke.webview.history.clear 消息
    status: completed
isProject: false
---

# 前端消息清除同步修复

## 问题分析

当前数据流对比：

```mermaid
flowchart LR
    subgraph webviewFlow [WebView 切换]
        A1[onModeChange] --> B1["messages.value = []"]
        B1 --> C1[postMessage 到后端]
    end
    
    subgraph cmdFlow [命令面板切换]
        A2[notifyModeChanged] --> B2["_messages = []"]
        B2 --> C2[_sendCurrentMode]
        C2 --> D2[前端更新 currentModeSlug]
        D2 -.-> E2["缺失: messages.value = []"]
    end
```

后端清除了 `_messages`，但前端的 `messages.value` 未被清除。

## 修复方案

在 `notifyModeChanged()` 中添加一条 `xiaoke.webview.history.clear` 消息，通知前端清除消息。

### 1. 修改 [webview-view-provider.ts](packages/extension/src/webview-view-provider.ts)

在 `notifyModeChanged()` 方法中添加发送清除历史的消息：

```typescript
public notifyModeChanged(): void {
  this._sendCurrentMode();
  this._messages = [];
  // 新增：通知前端清除聊天历史
  this._view?.webview.postMessage({
    command: "xiaoke.webview.history.clear",
  });
  console.log("模式已通过外部命令切换，UI 已同步，历史已清除");
}
```

### 2. 修改 [App3.vue](packages/webview-ui/src/App3.vue)

在 `vscodeListener` 的 switch 语句中添加处理 `xiaoke.webview.history.clear`：

```typescript
case 'xiaoke.webview.history.clear': {
  messages.value = [];
  break;
}
```

## 修改文件清单

- `packages/extension/src/webview-view-provider.ts`: 在 `notifyModeChanged()` 中发送清除消息
- `packages/webview-ui/src/App3.vue`: 处理 `xiaoke.webview.history.clear` 消息
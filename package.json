{
  "name": "vscode-tools-package",
  "displayName": "vscode-tools-package",
  "description": "",
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm --filter @vscode-tools/webview-ui build && pnpm --filter vscode-tools build",
    "build:ui": "pnpm --filter @vscode-tools/webview-ui build",
    "build:extension": "pnpm --filter vscode-tools build",
    "check": "biome check --write",
    "format": "biome format --write"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@rsbuild/core": "^1.4.0",
    "@types/node": "^18.x",
    "@types/vscode": "^1.82.0",
    "typescript": "^5.8.0",
    "ts-loader": "^9.5.0"
  }
}
